pipeline {
	agent none
	stages {
		stage('Build') {
			steps {
				echo 'build project'
			}
		}
		stage('Test') {
			steps {
				echo 'test project'
			}
		}
		stage('Publish') {
			steps {
				echo 'publish project'
			}
		}
	}
}